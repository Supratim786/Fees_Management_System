whenever sqlerror continue

drop table FEESSTRUCTURE;

whenever sqlerror exit failure;

CREATE TABLE FEESSTRUCTURE(
COURSE_NAME VARCHAR2(200),
SEMESTER_NAME VARCHAR2(200),
FEES_AMOUNT VARCHAR2(200),
FIRST_DATE_FEES_SUBMISSION VARCHAR2(200),

CONSTRAINT PK_FEESSTRUCTURE PRIMARY KEY (COURSE_NAME,SEMESTER_NAME)
);
